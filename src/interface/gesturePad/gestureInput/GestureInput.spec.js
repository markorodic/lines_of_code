// ********** TO TEST ************

// On mouse move and touch move, the current box position changes
// On mouse move and touch move, the expired boxes are added to the expired queue

// A pattern is sent to the gesturePad

// When a gesture is added, something happens in the code
// A valid gesture:
// provides visual feedback in the gesture pad
// provide visual feedback in the header
// changes the code

// An invalid gesture - doesn't do these things
